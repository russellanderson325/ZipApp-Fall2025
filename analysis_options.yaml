include: package:flutter_lints/flutter.yaml

#code analysis tool to help identify issues and enforce coding styles

analyzer:
  exclude:
    - lib-unused/**
    
linter:
  rules:
    - avoid_print
    - prefer_const_constructors
    - prefer_final_fields
    - use_key_in_widget_constructors